

export interface PaginationMetadata {
  total: number,
  page: number,
  size: number,
  previous: boolean,
  next: boolean,
  // --- start extra_fields code ---
  
  // --- end extra_fields code ---
}
